{
  "name": "kaoyan-pomodoro",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "check": "node scripts/check-syntax.mjs",
    "seed:demo": "node scripts/seed-demo-data.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
