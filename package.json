{
  "name": "kaoyan-pomodoro",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env server/index.js",
    "dev": "node --env-file-if-exists=.env --watch server/index.js",
    "backup": "node --env-file-if-exists=.env scripts/backup-database.mjs",
    "check": "node scripts/check-syntax.mjs && node scripts/check-css.mjs",
    "seed:demo": "node scripts/seed-demo-data.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
