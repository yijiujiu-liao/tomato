{
  "name": "kaoyan-pomodoro",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "check": "node --check script.js && node --check server/config.js && node --check server/index.js && node --check server/db.js && node --check server/auth.js && node --check tests/api.test.js",
    "test": "node --test tests/api.test.js"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
